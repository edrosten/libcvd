dependencies:
  pre:
  	- sudo add-apt-repository ppa:ubuntu-toolchain-r/test 
	- sudo apt-get update 
	- sudo apt-get install g++-7
  override:
  	- ./configure
	- make -j 4

test:
	override:
		- make test

